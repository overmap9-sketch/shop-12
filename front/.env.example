# Frontend (Next.js) env
# Copy to front/.env.local and adjust values as needed.

# Public site origin (used for canonical URLs, sitemap, Open Graph)
# Example: http://localhost:3000 or https://shop.example.com
PUBLIC_ORIGIN=http://localhost:3000

# Dev server port for Next.js
PORT=3000

# Backend origin used for API proxy rewrites (/api, /uploads)
# Example: http://localhost:4000
NEXT_PUBLIC_API_ORIGIN=http://localhost:4000

# Comma-separated list of additional origins allowed in dev (CORS/previews)
# Example: http://localhost:5173,https://preview.example.com
NEXT_PUBLIC_ALLOWED_DEV_ORIGINS=

# Disable Next.js telemetry (set to 1 to disable)
NEXT_TELEMETRY_DISABLED=1

# Stripe publishable key for frontend (pk_test_... in development)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
